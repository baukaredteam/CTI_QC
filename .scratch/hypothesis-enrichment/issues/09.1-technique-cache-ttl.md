# 09.1 — Technique cache TTL hardening

**Type:** task
**Status:** ready-for-agent
**Parent:** Ticket 09 `c26b6e6`
**Blocked by:** 09

**Scope:** production technique-cache TTL hardening. Split the single
`ThreadlinqsCache` TTL into two independent defaults — bundle cache stays
24 hours, technique cache becomes 7 days — so the production-style
construction `ThreadlinqsCache(redis)` matches the contract Ticket 09
already claimed ("miss -> `put_technique` with TTL 7 days (`tl:technique:*`)").

**Out of scope (this ticket):** Ticket 10 (frontend), Ticket 11 (GATE
smoke), Ticket 12 (docs handoff). No `scan_feed` wiring, no frontend, no
real MCP/network in tests.

**Decision state:** `.omo` inherited `NEEDS_DECISION` — owner decision needed
separately for live `scan_feed` prediction orchestration (recorded in
`09B-live-prediction-orchestration.md`, not implemented here).

**Root cause being fixed:** Ticket 09 added
`enricher.TECHNIQUE_CACHE_TTL_HOURS = 7 * 24`, but `ThreadlinqsCache` has a
single `ttl_hours` default of 24 hours applied to every cache entry, so
`enrich_predictions` -> `put_technique(technique_id, envelope)` writes the
technique under the bundle TTL. The existing test masked this by
constructing `ThreadlinqsCache(redis, ttl_hours=enricher.TECHNIQUE_CACHE_TTL_HOURS)`
— a test-only override that never matches production construction.

**Acceptance contract:** see `09.1-technique-cache-ttl.spec.md` (same dir).

## Comments

- Owner-approved unblock: 09.1 ticket/spec files were absent from the
  checkout; this file was created from the canonical contract in the
  Ticket 09.1 run prompt (2026-08-16).